name: ✔️ Codespell

on:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
    - name: 💾 Checkout
      uses: actions/checkout@v3
    - name: ✔️ Codespell
      uses: codespell-project/actions-codespell@master
